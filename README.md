# Reflector

## Usage


## Installation
```bash
git clone git@github.com:algodoogle/Reflector.git && \
cd ~/hobby/Reflector && \
python3 -m venv .venv && \
source .venv/bin/activate && \
pip install -U pip && \
pip install discord.py && \
```